import styled from "styled-components";
import { elementos } from "./elementosCalculadora";

const TecladoContainer = styled.section`

`

function Teclado() {
    return (
        <TecladoContainer>
            <section className="Teclado">

            </section>
        </TecladoContainer>
    )
}

export default Teclado